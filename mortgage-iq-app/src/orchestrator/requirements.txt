fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4

# Enable the FOUNDRY (real Azure AI Foundry) path. Optional for the mock demo.
azure-ai-projects==1.0.0b7
azure-identity==1.19.0
openai==1.59.6

# Enable the live Fabric IQ path (query the Fabric SQL analytics endpoint).
# Optional for the mock demo. Requires the "ODBC Driver 18 for SQL Server".
pyodbc==5.2.0
